@model FaleMais.Models.ADMIN

@{
    ViewBag.Title = "Edit";
}

<h2>Editar</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ADMIN</legend>
        @Html.HiddenFor(model => model.ADMIN_ID)
        <div class="editor-label">
            @Html.LabelFor(model => model.ADMIN_LOGIN)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ADMIN_LOGIN)
            @Html.ValidationMessageFor(model => model.ADMIN_LOGIN)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ADMIN_PASS)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ADMIN_PASS)
            @Html.ValidationMessageFor(model => model.ADMIN_PASS)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ADMIN_ATIVO)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ADMIN_ATIVO)
            @Html.ValidationMessageFor(model => model.ADMIN_ATIVO)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Voltar ", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
